# VCS + CI
.git
.gitignore
.github

# Docs + tooling not needed in the image
*.md
biome.json
tsconfig.base.json
railway.json
mise.toml
.dockerignore

# The web app (frontend) is deployed to Cloudflare, not baked into the API image
apps/web

# Agent / editor internals
.claude
.storecode
.vscode
.idea

# Tests
apps/api/test

# Dependencies + build output (installed / rebuilt inside the image)
node_modules
**/node_modules
